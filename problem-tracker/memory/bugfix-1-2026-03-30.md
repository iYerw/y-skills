# Bugfix 记录 - 主 Agent 测试发现问题

**日期**: 2026-03-30
**发现者**: 主 Agent（小爱同学）
**状态**: 需修复

---

## 问题描述

测试 add 命令时发现缺少 `--solution` 参数（设计文档要求有）。

主 Agent 自行尝试修复代码时出错：
```
sqlite3.OperationalError: 12 values for 11 columns
```

## 原因分析

- 主 Agent 在修改 INSERT 语句时，VALUES 有 12 个参数
- 但 CREATE TABLE 语句中列定义不完整

## 修复要求

请检查并确保：
1. CREATE TABLE 语句包含所有字段（包括 solution）
2. INSERT 语句的列数和参数数一致
3. add/update/show 命令都支持 solution 参数
4. 迁移逻辑正确（已有数据库添加 solution 列）

---

**教训**：主 Agent 不应直接改代码，应该派发给 coder。